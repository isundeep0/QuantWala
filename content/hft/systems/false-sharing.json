{
  "slug": "false-sharing",
  "sectionId": "systems",
  "order": 2,
  "title": "False Sharing & Cache-Line Contention",
  "tagline": "Two threads, two different variables, one shared cache line — and a silent 5–10x slowdown.",
  "level": "Advanced",
  "track": [
    "dev",
    "systems"
  ],
  "estMinutes": 16,
  "concept": {
    "analogy": "Imagine two clerks who each own a different document, but both documents are stapled to the same clipboard. Every time one clerk writes on her page, office rules force the whole clipboard to be handed over, so the other clerk must stop, wait, and re-grab it before touching his page. They never touch the same document, yet they constantly fight over the clipboard. The clipboard is the cache line; the documents are their separate variables.",
    "paragraphs": [
      "Cache coherence keeps the per-core caches consistent: when one core writes to a cache line, the protocol (MESI and friends) invalidates that line in every other core's cache. The coherence unit is the whole 64-byte line, not the individual variable.",
      "False sharing happens when two threads modify different variables that happen to live on the same cache line. Logically they're independent, but physically every write by one core invalidates the line in the other core's cache, forcing it to re-fetch the line (from L3 or the other core) before its own next access. The threads ping-pong the line back and forth, paying coherence traffic and stalls — often a 2–10x slowdown — with zero logical contention.",
      "It is insidious because the code looks correct and contention-free: no shared variable, no lock. It shows up as mysteriously poor scaling (adding threads doesn't help, or makes it worse) and high 'HITM' (hit-modified) counts in profilers. Classic victims: per-thread counters in an array, adjacent atomics, a producer's write index sitting next to a consumer's read index, and hot fields packed beside contended ones.",
      "The fix is to keep independently-written data on separate cache lines via padding/alignment. In C++17, `alignas(std::hardware_destructive_interference_size)` (or just `alignas(64)`) on the hot members or per-thread slots does it. The cost is a little memory; the payoff is reclaiming linear scaling."
    ],
    "points": [
      "Coherence operates on the whole 64-byte line, so writes invalidate neighbors' copies of the line.",
      "False sharing = different variables on one line, ping-ponging between cores — no logical contention.",
      "Symptom: poor multicore scaling, high HITM/coherence traffic, no obvious lock.",
      "Fix: pad/align hot, independently-written data to its own cache line (`alignas(64)`).",
      "Common in per-thread counter arrays, adjacent atomics, and ring-buffer head/tail indices."
    ],
    "callouts": [
      {
        "type": "warn",
        "title": "Atomics don't save you",
        "body": "Two atomics on the same line still false-share. Atomicity is about correctness; it does nothing for the coherence ping-pong. Separate them onto different lines."
      },
      {
        "type": "tip",
        "title": "Producer/consumer indices",
        "body": "In an SPSC ring buffer, put the head index and the tail index on different cache lines. Otherwise every producer write invalidates the consumer's line and vice versa — the single biggest false-sharing bug in lock-free queues."
      }
    ],
    "keyIdea": "Because coherence invalidates entire 64-byte lines, independent variables that share a line force cores to ping-pong it on every write. Pad/align hot, separately-written data onto its own line and the phantom contention disappears."
  },
  "deepDive": {
    "intro": "How MESI turns innocent adjacency into a stall, how to detect it, and how to pad it away correctly.",
    "steps": [
      {
        "title": "MESI in one paragraph",
        "detail": "Each cached line has a state: Modified, Exclusive, Shared, or Invalid. To write, a core must own the line Exclusive/Modified, which forces every other copy to Invalid. So a write by core A to a line that core B holds sends an invalidation; B's next read then misses and re-fetches the (now dirty) line — a coherence miss. Repeat per write and you get a ping-pong."
      },
      {
        "title": "The classic counter-array bug",
        "detail": "`long counters[NUM_THREADS];` with thread i doing `counters[i]++` looks perfectly parallel, but 8 longs fit in one 64-byte line, so all threads hammer the same line. Each increment invalidates the others. Replace with a padded slot per thread so each counter owns a line."
      },
      {
        "title": "Detecting it",
        "detail": "On Linux, `perf c2c record`/`perf c2c report` is purpose-built: it pinpoints cache lines suffering HITM (loads that hit a modified line on another core) and the offending offsets. High coherence/HITM with low logical sharing is the signature. VTune has a similar 'false sharing' analysis."
      },
      {
        "title": "Fixing with alignment/padding",
        "detail": "Use `alignas(std::hardware_destructive_interference_size)` (commonly 64) on the struct or hot member so each instance starts on its own line; or insert explicit padding bytes. For arrays of per-thread state, make each element a cache-line-sized aligned struct. Verify the size is a multiple of the line size so element i+1 also starts cleanly."
      },
      {
        "title": "The other side: true sharing & 'constructive' packing",
        "detail": "There's also hardware_constructive_interference_size — data you want together on one line (e.g., a tiny header read as a unit). Don't blindly pad everything: padding read-mostly data wastes cache. Pad only data that is frequently written by different cores."
      }
    ],
    "codes": [
      {
        "language": "cpp",
        "title": "false_sharing.cpp — the bug and the fix",
        "source": "#include <atomic>\n#include <thread>\n#include <vector>\n#include <new>   // std::hardware_destructive_interference_size\n\nconstexpr int T = 4;\nconstexpr long ITERS = 50'000'000;\n\n// BAD: 4 counters share one or two cache lines -> ping-pong on every ++\nstruct Bad { std::atomic<long> c[T]; };\n\n// GOOD: each counter sits alone on its own cache line\nstruct alignas(64) Padded { std::atomic<long> c{0}; };\nstruct Good { Padded c[T]; };\n\ntemplate <class F>\nvoid run(F&& bump) {\n    std::vector<std::thread> th;\n    for (int i = 0; i < T; ++i) th.emplace_back(bump, i);\n    for (auto& t : th) t.join();\n}\n\nint main() {\n    Bad bad;  for (auto& a : bad.c) a = 0;\n    run([&](int i){ for (long k=0;k<ITERS;++k) bad.c[i].fetch_add(1, std::memory_order_relaxed); });\n\n    Good good;\n    run([&](int i){ for (long k=0;k<ITERS;++k) good.c[i].c.fetch_add(1, std::memory_order_relaxed); });\n    // 'good' typically finishes several times faster: no inter-core invalidation.\n}"
      },
      {
        "language": "cpp",
        "title": "ring_indices.cpp — separate head and tail",
        "source": "#include <atomic>\n#include <cstddef>\n\n// In an SPSC queue, producer writes `tail`, consumer writes `head`.\n// Keep them on different cache lines so they don't invalidate each other.\nstruct Indices {\n    alignas(64) std::atomic<size_t> head{0};   // owned by consumer\n    alignas(64) std::atomic<size_t> tail{0};   // owned by producer\n};\n// Without alignas, head & tail share a line and every push invalidates the\n// consumer's cached index (and every pop invalidates the producer's) -> ping-pong."
      }
    ],
    "notes": [
      {
        "title": "alignas on heap allocations",
        "detail": "`new T` respects over-aligned types since C++17, and you can use aligned allocators / `std::pmr`. For arrays, ensure the element stride is a line multiple so no element straddles or shares lines."
      },
      {
        "title": "It can hide in the standard library",
        "detail": "Adjacent fields in a struct, small objects packed by a container, or two `std::atomic_flag`s side by side can all false-share. Lay out hot concurrent fields deliberately."
      }
    ],
    "pitfalls": [
      "Per-thread counters/state in a tight array without padding — the textbook false-sharing bug.",
      "Putting a ring buffer's head and tail (or producer/consumer cursors) on the same line.",
      "Assuming atomics or relaxed ordering avoid the cost — they don't; it's a coherence issue, not a correctness one.",
      "Over-padding read-mostly data, wasting cache capacity for no benefit.",
      "Padding to 64 but allocating the object unaligned, so it still straddles a line boundary."
    ]
  },
  "benchmark": {
    "intro": "Four threads each incrementing their own counter 50M times — shared line vs padded.",
    "columns": [
      "Layout",
      "Relative time",
      "Scaling",
      "Why"
    ],
    "rows": [
      [
        "counters[4] (shared line)",
        "~5–8x",
        "Negative / flat",
        "Every ++ invalidates the line in 3 other cores"
      ],
      {
        "cells": [
          "Padded alignas(64) per counter",
          "~1x",
          "Near-linear",
          "Each counter owns its line; no cross-core invalidation"
        ],
        "best": true
      }
    ],
    "note": "Exact ratio depends on core count and topology; the qualitative gap is large and reproducible.",
    "takeaway": "Same logic, same atomics — only the memory layout changed. Padding independently-written data to separate cache lines restored linear scaling and a multi-x speedup."
  },
  "practice": {
    "interviewQuestions": [
      {
        "id": "q-define",
        "q": "What is false sharing, and how is it different from a normal data race or true contention?",
        "tag": "Systems",
        "a": [
          "False sharing is a performance problem (not a correctness one) where two threads write to different variables that happen to occupy the same 64-byte cache line. Because cache coherence invalidates the whole line on a write, each thread's write evicts the line from the other core's cache, forcing re-fetches — they ping-pong the line despite never touching the same variable.",
          "A data race is a correctness bug (concurrent unsynchronized access to the same variable). True contention is multiple threads actually fighting over the same variable/lock. False sharing has neither: the program is correct and the variables are logically independent — only their physical adjacency causes the slowdown. That's why it's so easy to miss in code review."
        ],
        "followups": [
          "How would you detect it on Linux?",
          "Why don't atomics fix it?"
        ]
      },
      {
        "id": "q-fix",
        "q": "You have `struct Stats { std::atomic<long> hits; std::atomic<long> misses; };` updated by two different threads, and it scales badly. Fix it.",
        "tag": "C++",
        "a": [
          "`hits` and `misses` almost certainly share a cache line, so the hits-thread and misses-thread invalidate each other on every update. Separate them onto distinct lines: `struct Stats { alignas(std::hardware_destructive_interference_size) std::atomic<long> hits; alignas(...) std::atomic<long> misses; };` (or `alignas(64)`).",
          "Now each atomic owns its own line and the cross-core invalidations stop. If updates are very frequent and you only need the total occasionally, an even better design is per-thread local counters (each padded) summed lazily on read — eliminating both contention and false sharing."
        ]
      },
      {
        "id": "q-find-line-size",
        "q": "How would you determine the cache line size at runtime/build time, and why does code sometimes hardcode 64?",
        "tag": "Systems",
        "a": [
          "At build/run time you can query it: on Linux read /sys/devices/system/cpu/cpu0/cache/index0/coherency_line_size or use sysconf(_SC_LEVEL1_DCACHE_LINESIZE); in C++17 there's std::hardware_destructive_interference_size (the recommended portable padding hint) and std::hardware_constructive_interference_size. The CPUID instruction also reports it on x86.",
          "Code often hardcodes 64 because virtually all modern x86-64 and most ARM server cores use a 64-byte line, so `alignas(64)` is a safe, simple default for padding. The caveat: some platforms differ (e.g., Apple M-series and some POWER use 128-byte lines, and some CPUs prefetch pairs of lines effectively making 128 the practical unit), so for portability prefer std::hardware_destructive_interference_size or a configured constant. The practical guidance: hardcode 64 for x86-only low-latency code, but use the portable constant when targeting multiple architectures so padding actually separates contended data."
        ],
        "followups": [
          "What is hardware_constructive_interference_size for?",
          "Could padding to 64 ever be insufficient?"
        ]
      }
    ],
    "problems": [
      {
        "id": "p-detect",
        "title": "Find the false sharing",
        "difficulty": "Intermediate",
        "statement": "A worker pool aggregates results into `struct Acc { long sum; long count; } accs[NTHREADS];`, each thread writing only `accs[tid]`. CPU usage is high but throughput barely improves past 2 threads. Identify the bug, propose a fix, and describe how you'd prove the fix worked.",
        "hint": "How many Acc structs fit in a 64-byte line?",
        "tests": "Recognizing and fixing array-based false sharing.",
        "solution": {
          "approach": [
            "Each Acc is 16 bytes, so four of them fit in one 64-byte line. Threads 0–3 all write to the same line; each write invalidates the others' cached copy, so they serialize on coherence traffic instead of running in parallel — hence flat scaling.",
            "Fix: make each slot own a line — `struct alignas(64) Acc { long sum; long count; };` (or pad to 64 bytes). Better yet, give each thread a private local Acc on its own stack/line and merge at the end, avoiding shared writes entirely during the hot loop.",
            "Prove it with `perf c2c record`/`report` before and after (HITM on that line should vanish), and by showing throughput now scales roughly linearly with thread count up to the core limit."
          ],
          "complexity": {
            "time": "Restores ~linear scaling",
            "space": "+padding per slot (line-sized)"
          },
          "codes": [
            {
              "language": "cpp",
              "title": "fix.cpp",
              "source": "struct alignas(64) Acc { long sum = 0; long count = 0; };\nstd::vector<Acc> accs(NTHREADS);   // each Acc starts on its own cache line\n// thread tid: accs[tid].sum += x; accs[tid].count++;  // no cross-core invalidation"
            }
          ]
        }
      },
      {
        "id": "p-scale-debug",
        "title": "Debug a multithreaded throughput collapse",
        "difficulty": "Advanced",
        "statement": "A lock-free SPSC queue between two pinned threads gives great throughput, but when you add per-thread statistics counters in a shared `struct Stats { std::atomic<long> produced; std::atomic<long> consumed; };` (producer bumps `produced`, consumer bumps `consumed`), throughput drops ~40%. Diagnose the cause, fix it, and describe how you'd confirm the diagnosis.",
        "hint": "`produced` and `consumed` almost certainly share a cache line; the two cores ping-pong it on every bump.",
        "tests": "Recognizing false sharing introduced by 'harmless' instrumentation.",
        "solution": {
          "approach": [
            "Diagnosis: `produced` and `consumed` are adjacent in the struct, so they almost certainly occupy the same 64-byte cache line. The producer core writes `produced` every push and the consumer core writes `consumed` every pop; each write invalidates the other core's copy of the shared line (cache-coherence ping-pong), so both threads stall re-fetching the line — classic false sharing introduced by 'innocent' instrumentation. The queue itself was fine; the counters coupled the two cores through one line.",
            "Fix: put each counter on its own cache line — `struct Stats { alignas(std::hardware_destructive_interference_size) std::atomic<long> produced; alignas(...) std::atomic<long> consumed; };` (or alignas(64)). Even better, keep per-thread *local* (non-atomic) counters bumped without any sharing and only merge them occasionally for reporting, eliminating both contention and false sharing on the hot path.",
            "Confirm: run `perf c2c record`/`report` before and after — you should see HITM (hit-modified) coherence traffic on the Stats line before the fix and none after; and throughput should return to the no-counter baseline once the counters are line-separated or made thread-local. This is a textbook case of measurement code silently destroying scaling via false sharing."
          ],
          "complexity": {
            "time": "restores baseline throughput",
            "space": "+padding per counter"
          }
        }
      }
    ]
  },
  "resources": [
    {
      "title": "perf c2c: shared data / false sharing analysis",
      "by": "Joe Mario (Red Hat)",
      "note": "The standard Linux tool and write-up for hunting false sharing."
    },
    {
      "title": "std::hardware_destructive_interference_size",
      "note": "C++17 portable cache-line padding hint (cppreference)."
    }
  ]
}
