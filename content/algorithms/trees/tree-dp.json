{
  "slug": "tree-dp",
  "categoryId": "trees",
  "order": 4,
  "title": "DP on Trees",
  "tagline": "Compute each node's answer from its children — subtree DP and rerooting.",
  "estMinutes": 20,
  "simulator": null,
  "complexity": { "time": "O(n)", "space": "O(h)" },
  "tags": ["dp", "post-order", "rerooting"],
  "intuition": {
    "analogy": "A company org chart: each manager's headcount equals the sum of their reports' headcounts plus one. You can only know a manager's total after every report has reported up — bottom-up aggregation.",
    "paragraphs": [
      "Tree DP solves problems where a node's answer depends on the answers of its subtrees. Because subtrees are independent, a single post-order DFS computes everything: visit children first, then combine their results at the parent.",
      "Many problems carry a small state per node — e.g. 'best if this node is included' vs 'best if excluded' (House Robber III), or counts, sums, and depths. The combine step is where the actual recurrence lives.",
      "When the answer is needed for every node as a root (not just one root), the rerooting technique computes the rooted answer once, then transfers information from parent to child in a second DFS to adjust each node's perspective in O(n) total."
    ],
    "keyIdea": "Post-order DFS aggregates child results into each node; rerooting extends per-node answers to all roots in O(n)."
  },
  "logic": {
    "steps": [
      { "title": "Define the per-node state", "detail": "Decide what each subtree returns — a single number, or a tuple like (best including node, best excluding node)." },
      { "title": "Write the combine rule", "detail": "Express the parent's state from its children's states. This recurrence is the crux of every tree-DP problem." },
      { "title": "Aggregate post-order", "detail": "DFS down, compute children, then combine. Track a global answer if the problem asks for a path/optimum across the tree." },
      { "title": "Reroot if needed", "detail": "For 'answer for every root' questions, do a second DFS pushing the complement information from parent to each child." }
    ],
    "code": {
      "language": "python",
      "title": "house_robber_iii.py",
      "source": "def rob(root):\n    def dfs(node):\n        if not node: return (0, 0)        # (rob, skip)\n        l = dfs(node.left)\n        r = dfs(node.right)\n        rob_here = node.val + l[1] + r[1]  # can't rob children\n        skip_here = max(l) + max(r)        # children free\n        return (rob_here, skip_here)\n    return max(dfs(root))"
    },
    "complexity": {
      "time": "O(n)",
      "timeReason": "Each node combines a constant amount of child information once.",
      "space": "O(h)",
      "spaceReason": "Recursion depth equals the tree height."
    },
    "pitfalls": [
      "Returning the global answer from the recursion instead of the per-node state needed by parents.",
      "Forgetting the empty-child base case.",
      "Rerooting bugs: mixing up which neighbour's contribution to exclude when moving the root."
    ]
  },
  "dryRun": {
    "input": "House Robber III on tree 3-(2-(null,3))-(3-(null,1))",
    "columns": ["Node", "(rob, skip)", "Note"],
    "rows": [
      ["leaf 3", "(3, 0)", "rob leaf = its value"],
      ["leaf 1", "(1, 0)", "—"],
      ["2", "(2+0, 3) = (2,3)", "skip uses child's best"],
      ["root 3", "(3+3+0, 3+1)=(6,4)", "answer = max(6,4)=7"]
    ],
    "clickMoment": "Each node keeps two numbers, not one. Carrying both 'include' and 'exclude' lets the parent pick freely — that extra state is what makes tree DP expressive."
  },
  "problems": [
    { "id": "tdp1", "title": "House Robber III", "difficulty": "Medium", "statement": "Max sum with no two directly-linked nodes chosen.", "hint": "Return (rob, skip) per node.", "tests": "Two-state subtree DP.", "link": "https://leetcode.com/problems/house-robber-iii/" },
    { "id": "tdp2", "title": "Binary Tree Cameras", "difficulty": "Hard", "statement": "Minimum cameras to monitor all nodes.", "hint": "Three states per node: covered+camera, covered+no-camera, not-covered. Greedy bottom-up.", "tests": "Multi-state tree DP.", "link": "https://leetcode.com/problems/binary-tree-cameras/" },
    { "id": "tdp3", "title": "Count Good Nodes in Binary Tree", "difficulty": "Medium", "statement": "Count nodes ≥ every ancestor on their path.", "hint": "Carry the running path maximum downward.", "tests": "Top-down accumulation.", "link": "https://leetcode.com/problems/count-good-nodes-in-binary-tree/" },
    { "id": "tdp4", "title": "Distribute Coins in Binary Tree", "difficulty": "Medium", "statement": "Minimum moves to give each node exactly one coin.", "hint": "Each edge carries |subtree balance|; sum these in post-order.", "tests": "Subtree balance aggregation.", "link": "https://leetcode.com/problems/distribute-coins-in-binary-tree/" },
    { "id": "tdp5", "title": "Sum of Distances in Tree", "difficulty": "Hard", "statement": "For each node, the sum of distances to all others.", "hint": "Classic rerooting: compute for the root, then adjust per child in a second DFS.", "tests": "Rerooting technique.", "link": "https://leetcode.com/problems/sum-of-distances-in-tree/" }
  ]
}
