{
  "slug": "sieve",
  "categoryId": "math-number-theory",
  "order": 1,
  "title": "Sieve of Eratosthenes",
  "tagline": "Cross out multiples to find every prime up to N in O(N log log N).",
  "estMinutes": 14,
  "simulator": "sieve",
  "complexity": { "time": "O(N log log N)", "space": "O(N)" },
  "tags": ["primes", "number theory", "preprocessing"],
  "intuition": {
    "analogy": "Walk down the number line. The first uncrossed number (2) is prime — now cross out every second number after it. Move to the next uncrossed number (3), cross out every third, and so on. Whatever survives is prime.",
    "paragraphs": [
      "Testing each number for primality individually is slow. The sieve flips the logic: instead of asking 'is n prime?', it eliminates composites wholesale by crossing out the multiples of each prime it finds.",
      "Two optimisations make it fast. Start crossing out from p² (smaller multiples already had a smaller prime factor crossing them), and only sieve primes up to √N (any composite ≤ N has a factor ≤ √N).",
      "The running time O(N log log N) is nearly linear — the log log factor comes from summing 1/p over primes. The sieve is the foundation for factorisation tables, Euler's totient, and divisor counts."
    ],
    "keyIdea": "Each prime erases its multiples; survivors are prime. Start at p² and sieve only up to √N."
  },
  "logic": {
    "steps": [
      { "title": "Assume all prime", "detail": "isPrime[2..N] = true; 0 and 1 are not prime." },
      { "title": "Cross out multiples", "detail": "For p from 2 to √N, if isPrime[p], mark p², p²+p, p²+2p, … as composite." },
      { "title": "Collect", "detail": "Indices still true are primes. Optionally store smallest prime factors for fast factorisation." }
    ],
    "code": {
      "language": "cpp",
      "title": "sieve.cpp",
      "source": "vector<bool> sieve(int N) {\n    vector<bool> isPrime(N + 1, true);\n    isPrime[0] = isPrime[1] = false;\n    for (int p = 2; (long long)p * p <= N; ++p)\n        if (isPrime[p])\n            for (int m = p * p; m <= N; m += p)\n                isPrime[m] = false;\n    return isPrime;\n}"
    },
    "complexity": {
      "time": "O(N log log N)",
      "timeReason": "Summing N/p over primes p ≤ N gives the near-linear log log bound.",
      "space": "O(N)",
      "spaceReason": "A boolean array of size N (bitset halves the memory)."
    },
    "pitfalls": [
      "Start the inner loop at p·p, not 2·p, to avoid redundant work.",
      "Use long long for p·p when N is large to avoid overflow.",
      "For a single primality test of a huge number, use Miller-Rabin instead — the sieve needs O(N) memory."
    ]
  },
  "dryRun": {
    "input": "N = 12",
    "columns": ["Prime p", "Cross out", "Remaining primes"],
    "rows": [
      ["2", "4,6,8,10,12", "2,3,5,7,9,11"],
      ["3", "9 (6,12 already gone)", "2,3,5,7,11"],
      ["√12<4", "stop", "2,3,5,7,11"]
    ],
    "clickMoment": "When sieving 3, the number 9 = 3² is the first NEW composite it removes — 6 and 12 were already crossed by 2. Starting at p² avoids re-crossing those."
  },
  "problems": [
    { "id": "sv1", "title": "Count Primes", "difficulty": "Medium", "statement": "Count primes strictly less than n.", "hint": "Sieve then count true entries.", "tests": "Core sieve.", "link": "https://leetcode.com/problems/count-primes/" },
    { "id": "sv2", "title": "Closest Prime Numbers in Range", "difficulty": "Medium", "statement": "Find the closest pair of primes in [left, right].", "hint": "Sieve the range, scan consecutive primes.", "tests": "Sieve + scan.", "link": "https://leetcode.com/problems/closest-prime-numbers-in-range/" },
    { "id": "sv3", "title": "Prime Factorization (SPF)", "difficulty": "Medium", "statement": "Factorize many numbers quickly.", "hint": "Sieve storing the smallest prime factor of each number.", "tests": "Smallest-prime-factor sieve.", "link": "https://www.geeksforgeeks.org/smallest-prime-factor-of-numbers-till-n/" },
    { "id": "sv4", "title": "Distinct Prime Factors Count", "difficulty": "Medium", "statement": "Count distinct primes across a product range.", "hint": "Sieve-based factor counting.", "tests": "Sieve application.", "link": "https://leetcode.com/problems/distinct-prime-factors-of-product-of-array/" },
    { "id": "sv5", "title": "Ugly Number / Smooth Numbers", "difficulty": "Medium", "statement": "Generate numbers with only small prime factors.", "hint": "Sieve-like multiplicative generation.", "tests": "Multiplicative sieving idea.", "link": "https://leetcode.com/problems/ugly-number-ii/" }
  ]
}
