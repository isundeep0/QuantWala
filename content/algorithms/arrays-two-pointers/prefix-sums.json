{
  "slug": "prefix-sums",
  "categoryId": "arrays-two-pointers",
  "order": 3,
  "title": "Prefix Sums",
  "tagline": "Precompute running totals once; answer any range-sum query in O(1).",
  "estMinutes": 16,
  "simulator": "prefix-sums",
  "complexity": {
    "time": "O(n) build, O(1) query",
    "space": "O(n)"
  },
  "tags": [
    "preprocessing",
    "range query",
    "hashing"
  ],
  "intuition": {
    "analogy": "A bank statement lists your running balance after every transaction. To know how much you spent between Tuesday and Friday, you don't re-add every purchase — you subtract Tuesday's balance from Friday's. The running balance is a prefix sum.",
    "paragraphs": [
      "If you'll be asked many 'sum of elements from i to j' questions, recomputing each from scratch is O(n) per query. Prefix sums pay O(n) once to precompute cumulative totals, after which any range sum is a single subtraction.",
      "Define pre[k] = a[0] + a[1] + … + a[k-1] (with pre[0] = 0). Then the sum of a[l..r] equals pre[r+1] - pre[l]. The off-by-one is tamed by the extra leading zero.",
      "The idea generalises: prefix XOR, prefix products, 2D prefix sums for submatrix queries, and — most powerfully — combining prefix sums with a hash map to count subarrays whose sum equals a target in O(n)."
    ],
    "keyIdea": "Range sum [l, r] = pre[r+1] − pre[l]. Precompute once, query forever in O(1)."
  },
  "logic": {
    "steps": [
      {
        "title": "Build the prefix array",
        "detail": "pre[0] = 0; for i from 1 to n, pre[i] = pre[i-1] + a[i-1]. The leading zero makes range math clean."
      },
      {
        "title": "Answer queries",
        "detail": "Sum of a[l..r] = pre[r+1] − pre[l]. Constant time per query."
      },
      {
        "title": "Hash-map extension",
        "detail": "To count subarrays with sum = k, walk prefix sums and look up how many earlier prefixes equal current − k."
      }
    ],
    "codes": [
      {
        "language": "cpp",
        "title": "subarray_sum_equals_k.cpp",
        "source": "// Count subarrays whose sum equals k using prefix sums + a hashmap.\nint subarraySum(vector<int>& nums, int k) {\n    unordered_map<long long,int> seen{{0, 1}};   // empty prefix\n    long long prefix = 0; int count = 0;\n    for (int x : nums) {\n        prefix += x;\n        if (seen.count(prefix - k)) count += seen[prefix - k];\n        seen[prefix]++;\n    }\n    return count;\n}"
      },
      {
        "language": "python",
        "title": "subarray_sum_equals_k.py",
        "source": "def subarray_sum(nums, k):\n    count = 0\n    prefix = 0\n    seen = {0: 1}                 # empty prefix\n    for x in nums:\n        prefix += x\n        count += seen.get(prefix - k, 0)\n        seen[prefix] = seen.get(prefix, 0) + 1\n    return count"
      }
    ],
    "complexity": {
      "time": "O(n) build, O(1) per range query",
      "timeReason": "One linear pass builds the array; each query is one subtraction.",
      "space": "O(n)",
      "spaceReason": "Stores the prefix array (or hash map of seen prefixes)."
    },
    "pitfalls": [
      "Off-by-one: with pre[0]=0, the range [l, r] uses pre[r+1] − pre[l], not pre[r] − pre[l].",
      "For the hash-map count trick, seed the map with {0: 1} to count subarrays starting at index 0.",
      "Watch for integer overflow with large arrays — use 64-bit sums."
    ]
  },
  "dryRun": {
    "input": "a = [3, 1, 4, 1, 5, 9, 2, 6], query [2, 5]",
    "columns": [
      "Step",
      "State",
      "Note"
    ],
    "rows": [
      [
        "build",
        "pre = [0,3,4,8,9,14,23,25,31]",
        "Cumulative totals with leading 0"
      ],
      [
        "query",
        "pre[6] − pre[2]",
        "Range [2,5] uses indices r+1=6 and l=2"
      ],
      [
        "result",
        "23 − 4 = 19",
        "Sum of a[2..5] = 4+1+5+9 = 19"
      ]
    ],
    "clickMoment": "The single subtraction pre[6] − pre[2] replaced summing four elements. With a thousand queries, that's the difference between O(1000·n) and O(n + 1000)."
  },
  "problems": [
    {
      "id": "ps1",
      "title": "Range Sum Query - Immutable",
      "difficulty": "Easy",
      "statement": "Answer many range-sum queries on a fixed array.",
      "hint": "Build a prefix array in the constructor; query in O(1).",
      "tests": "Core prefix-sum array.",
      "link": "https://leetcode.com/problems/range-sum-query-immutable/",
      "solution": {
        "approach": [
          "Precompute pre[i] = a[0]+...+a[i-1]. Any range sum [l, r] is pre[r+1] - pre[l] in O(1).",
          "Build the prefix array once in the constructor; each query is a single subtraction."
        ],
        "complexity": {
          "time": "O(n) build, O(1) query",
          "space": "O(n)"
        },
        "codes": [
          {
            "language": "cpp",
            "title": "solution.cpp",
            "source": "class NumArray {\n    vector<long long> pre;\npublic:\n    NumArray(vector<int>& a) {\n        pre.assign(a.size() + 1, 0);\n        for (int i = 0; i < (int)a.size(); i++) pre[i + 1] = pre[i] + a[i];\n    }\n    int sumRange(int l, int r) { return (int)(pre[r + 1] - pre[l]); }\n};"
          },
          {
            "language": "python",
            "title": "solution.py",
            "source": "from itertools import accumulate\nclass NumArray:\n    def __init__(self, a: list[int]):\n        self.pre = [0] + list(accumulate(a))\n    def sumRange(self, l: int, r: int) -> int:\n        return self.pre[r + 1] - self.pre[l]"
          }
        ]
      }
    },
    {
      "id": "ps2",
      "title": "Subarray Sum Equals K",
      "difficulty": "Medium",
      "statement": "Count subarrays summing exactly to k.",
      "hint": "Prefix sum + hash map of seen prefix counts.",
      "tests": "Prefix sums combined with hashing.",
      "link": "https://leetcode.com/problems/subarray-sum-equals-k/",
      "solution": {
        "approach": [
          "A subarray sums to k iff pre[j] - pre[i] = k, i.e. pre[i] = pre[j] - k. Count, with a hash map, how many earlier prefixes equal pre[j] - k.",
          "Stream the prefix sum, look up (prefix - k) in the map, then record the current prefix. O(n)."
        ],
        "complexity": {
          "time": "O(n)",
          "space": "O(n)"
        },
        "codes": [
          {
            "language": "cpp",
            "title": "solution.cpp",
            "source": "class Solution {\npublic:\n    int subarraySum(vector<int>& a, int k) {\n        unordered_map<int,int> cnt{{0, 1}};\n        int pre = 0, ans = 0;\n        for (int x : a) { pre += x; ans += cnt[pre - k]; cnt[pre]++; }\n        return ans;\n    }\n};"
          },
          {
            "language": "python",
            "title": "solution.py",
            "source": "from collections import defaultdict\nclass Solution:\n    def subarraySum(self, a: list[int], k: int) -> int:\n        cnt = defaultdict(int); cnt[0] = 1\n        pre = ans = 0\n        for x in a:\n            pre += x\n            ans += cnt[pre - k]\n            cnt[pre] += 1\n        return ans"
          }
        ]
      }
    },
    {
      "id": "ps3",
      "title": "Contiguous Array",
      "difficulty": "Medium",
      "statement": "Longest subarray with equal 0s and 1s.",
      "hint": "Treat 0 as -1; equal counts means equal prefix sums — store first occurrence.",
      "tests": "Prefix transform + first-seen map.",
      "link": "https://leetcode.com/problems/contiguous-array/",
      "solution": {
        "approach": [
          "Map 0 -> -1 and 1 -> +1; then a subarray with equal 0s and 1s has prefix sum difference 0, i.e. two equal prefix sums.",
          "Store the first index where each prefix value appears; the longest span is the current index minus that earliest index."
        ],
        "complexity": {
          "time": "O(n)",
          "space": "O(n)"
        },
        "codes": [
          {
            "language": "cpp",
            "title": "solution.cpp",
            "source": "class Solution {\npublic:\n    int findMaxLength(vector<int>& a) {\n        unordered_map<int,int> first{{0, -1}};\n        int pre = 0, best = 0;\n        for (int i = 0; i < (int)a.size(); i++) {\n            pre += a[i] ? 1 : -1;\n            if (first.count(pre)) best = max(best, i - first[pre]);\n            else first[pre] = i;\n        }\n        return best;\n    }\n};"
          },
          {
            "language": "python",
            "title": "solution.py",
            "source": "class Solution:\n    def findMaxLength(self, a: list[int]) -> int:\n        first = {0: -1}\n        pre = best = 0\n        for i, x in enumerate(a):\n            pre += 1 if x else -1\n            if pre in first:\n                best = max(best, i - first[pre])\n            else:\n                first[pre] = i\n        return best"
          }
        ]
      }
    },
    {
      "id": "ps4",
      "title": "Range Sum Query 2D - Immutable",
      "difficulty": "Medium",
      "statement": "Submatrix sum queries on a fixed matrix.",
      "hint": "2D prefix sums; inclusion-exclusion of four corners.",
      "tests": "2D prefix sums.",
      "link": "https://leetcode.com/problems/range-sum-query-2d-immutable/",
      "solution": {
        "approach": [
          "Build a 2D prefix where pre[i][j] is the sum of the rectangle from (0,0) to (i-1,j-1).",
          "Any submatrix sum uses inclusion-exclusion: pre[r2+1][c2+1] - pre[r1][c2+1] - pre[r2+1][c1] + pre[r1][c1]."
        ],
        "complexity": {
          "time": "O(mn) build, O(1) query",
          "space": "O(mn)"
        },
        "codes": [
          {
            "language": "cpp",
            "title": "solution.cpp",
            "source": "class NumMatrix {\n    vector<vector<long long>> pre;\npublic:\n    NumMatrix(vector<vector<int>>& m) {\n        int R = m.size(), C = m[0].size();\n        pre.assign(R + 1, vector<long long>(C + 1, 0));\n        for (int i = 0; i < R; i++)\n            for (int j = 0; j < C; j++)\n                pre[i+1][j+1] = m[i][j] + pre[i][j+1] + pre[i+1][j] - pre[i][j];\n    }\n    int sumRegion(int r1, int c1, int r2, int c2) {\n        return (int)(pre[r2+1][c2+1] - pre[r1][c2+1] - pre[r2+1][c1] + pre[r1][c1]);\n    }\n};"
          },
          {
            "language": "python",
            "title": "solution.py",
            "source": "class NumMatrix:\n    def __init__(self, m: list[list[int]]):\n        R, C = len(m), len(m[0])\n        self.pre = [[0] * (C + 1) for _ in range(R + 1)]\n        for i in range(R):\n            for j in range(C):\n                self.pre[i+1][j+1] = m[i][j] + self.pre[i][j+1] + self.pre[i+1][j] - self.pre[i][j]\n    def sumRegion(self, r1, c1, r2, c2):\n        p = self.pre\n        return p[r2+1][c2+1] - p[r1][c2+1] - p[r2+1][c1] + p[r1][c1]"
          }
        ]
      }
    },
    {
      "id": "ps5",
      "title": "Maximum Size Subarray Sum Equals k",
      "difficulty": "Medium",
      "statement": "Longest subarray with sum exactly k.",
      "hint": "Store the earliest index for each prefix value; maximise length.",
      "tests": "Prefix sums for longest range.",
      "link": "https://leetcode.com/problems/maximum-size-subarray-sum-equals-k/",
      "solution": {
        "approach": [
          "Same identity as Subarray-Sum-Equals-K, but now maximise length: we need the earliest index with prefix value pre - k.",
          "Record the first occurrence of each prefix sum only; for each j check if pre - k was seen and update the best length."
        ],
        "complexity": {
          "time": "O(n)",
          "space": "O(n)"
        },
        "codes": [
          {
            "language": "cpp",
            "title": "solution.cpp",
            "source": "class Solution {\npublic:\n    int maxSubArrayLen(vector<int>& a, int k) {\n        unordered_map<long long,int> first{{0, -1}};\n        long long pre = 0; int best = 0;\n        for (int i = 0; i < (int)a.size(); i++) {\n            pre += a[i];\n            if (first.count(pre - k)) best = max(best, i - first[pre - k]);\n            if (!first.count(pre)) first[pre] = i;\n        }\n        return best;\n    }\n};"
          },
          {
            "language": "python",
            "title": "solution.py",
            "source": "class Solution:\n    def maxSubArrayLen(self, a: list[int], k: int) -> int:\n        first = {0: -1}\n        pre = best = 0\n        for i, x in enumerate(a):\n            pre += x\n            if pre - k in first:\n                best = max(best, i - first[pre - k])\n            if pre not in first:\n                first[pre] = i\n        return best"
          }
        ]
      }
    }
  ]
}
